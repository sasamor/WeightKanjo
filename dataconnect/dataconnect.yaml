specVersion: "v1beta"
serviceId: "my-expense-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-expense-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
